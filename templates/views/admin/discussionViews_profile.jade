extends ../../layouts/admin-default


block content

    .col-sm-9.col-sm-offset-3.col-md-10.col-md-offset-3.main 
        .row
            h3 #{data.discussion_views._id}
        .row
            .col-md-2
                p IP
            .col-md-3
                mark #{data.discussion_views.ip}
        .row
            .col-md-2
                p Time
            .col-md-3
                mark #{data.discussion_views.time}
        .row    
            .col-md-2
                p City
            .col-md-3
                mark #{data.discussion_views.city}
        .row    
            .col-md-2
                p Region
            .col-md-3   
                mark #{data.discussion_views.region}
        .row
            .col-md-2
                p Country
            .col-md-3    
                mark #{data.discussion_views.country}
        .row    
            .col-md-2
                p Loc
            .col-md-3    
                mark #{data.discussion_views.loc}
        br
        br
        footer.footer
            .container
                button.btn.btn-link(type='submit', data-toggle='modal', data-target='#deleteDiscussionView') Delete Discussion View
    #deleteDiscussionView.modal.fade(tabindex='-1', role='dialog', aria-labelledby='deleteDiscussionViewLabel')
        .modal-dialog(role='document')
            .modal-content
                .modal-header
                    button.close(type='button', data-dismiss='modal', aria-label='Close')
                        span(aria-hidden='true') ×
                    h4#deleteDiscussionViewLabel.modal-title #{'Are you sure you want to delete '+data.discussion_views._id+'?'}
                .modal-body
                    p This cannot be undone
                .modal-footer
                    form.form-group.row(method='post', action='/admin/discussion-views/'+data.discussion_views._id)
                        input(type='hidden', name='action', value='deleteDiscussionView')  
                        button.btn.btn-danger(type='submit', name='deleteDiscussionView') Delete
                        button.btn.btn-default(type='button', data-dismiss='modal') Close
