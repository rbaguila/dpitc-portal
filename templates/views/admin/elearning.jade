extends ../../layouts/admin-default

block content
    .row
        .col-sm-9.col-sm-offset-3.col-md-9.col-md-offset-2.main
            if data.path === '/admin/learning-objects'
                input.form-control(type='text', placeholder='Search..')
                button.btn.btn-success(type='button') Create Learning Object
                .table-responsive   
                    table.table.table-striped
                        thead
                        tr
                            th Title
                            th Author
                            th One-paragrapth Description
                            th State
                            th Published At
                        tbody
                        each lo in data.learning_objects
                            tr   
                                td 
                                    a(href='/admin/learning-objects/'+lo._id) #{lo.title}
                                td #{lo.author}
                                td #{lo.content.brief}
                                td #{lo.state}
                                td #{lo.publishedAt}
                        else
                                    .jumbotron
                                        h3 No data
            if data.path === '/admin/courses'
                input.form-control(type='text', placeholder='Search..')
                button.btn.btn-success(type='button') Create Course
                .table-responsive   
                    table.table.table-striped
                        thead
                        tr
                            th Title
                            th One-paragrapth Description
                            th State
                            th Published At
                        tbody
                        each courses in data.courses
                            tr   
                                td 
                                    a(href='/admin/courses/'+courses._id) #{courses.title}
                                td #{courses.content.brief}
                                td #{courses.state}
                                td #{courses.publishedAt}
                        else
                            .jumbotron
                                h3 No data
            if data.path === '/admin/learning-contents'
                input.form-control(type='text', placeholder='Search..')
                button.btn.btn-success(type='button') Create Learning Content
                .table-responsive   
                    table.table.table-striped
                        thead
                        tr
                            th Title
                            th One-paragrapth Description
                            th State
                            th Published At
                        tbody
                        each contents in data.learning_contents
                            tr   
                                td 
                                    a(href='/admin/learning-contents/'+ contents._id) #{contents.title}
                                td #{contents.content.brief}
                                td #{contents.state}
                                td #{contents.publishedAt}
                        else
                            .jumbotron
                                h3 No data
            if data.path === '/admin/isps'
                input.form-control(type='text', placeholder='Search..')
                button.btn.btn-success(type='button') Create ISP
                .table-responsive   
                    table.table.table-striped
                        thead
                        tr
                            th Name
                            th Sector
                        tbody
                        each isp in data.isps
                            tr
                                td 
                                    a(href='/admin/isps/'+ isp._id) #{isp.name}
                                td #{isp.sector}
                        else
                            .jumbotron
                                    h3 No data
            if data.path === '/admin/lindustries'
                input.form-control(type='text', placeholder='Search..')
                button.btn.btn-success(type='button') Create LIndustry
                .table-responsive   
                    table.table.table-striped
                        thead
                        tr
                            th Name
                        tbody
                        each lindustry in data.lindustries
                            tr
                                td 
                                    a(href='/admin/lindustries/'+ lindustry._id) #{lindustry.name}
                        else
                            .jumbotron
                                    h3 No data
            if data.path === '/admin/lsectors'
                input.form-control(type='text', placeholder='Search..')
                button.btn.btn-success(type='button') Create LSector
                .table-responsive   
                    table.table.table-striped
                        thead
                        tr
                            th Name
                            th Industry
                        tbody
                        each lsector in data.lsectors
                            tr
                                td 
                                    a(href='/admin/lsectors/'+ lsector._id) #{lsector.name}
                                td #{lsector.industry}
                        else
                            .jumbotron
                                    h3 No data
            