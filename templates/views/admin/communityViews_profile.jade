extends ../../layouts/admin-default


block content

    .col-sm-9.col-sm-offset-3.col-md-10.col-md-offset-3.main 
        .row
            h3 #{data.community_views._id}
        .row
            .col-md-2
                p IP
            .col-md-3
                mark #{data.community_views.ip}
        .row
            .col-md-2
                p Time
            .col-md-3
                mark #{data.community_views.time}
        .row    
            .col-md-2
                p City
            .col-md-3
                mark #{data.community_views.city}
        .row    
            .col-md-2
                p Region
            .col-md-3   
                mark #{data.community_views.region}
        .row
            .col-md-2
                p Country
            .col-md-3    
                mark #{data.community_views.country}
        .row    
            .col-md-2
                p Loc
            .col-md-3    
                mark #{data.community_views.loc}
        br
        br
        footer.footer
            .container
                button.btn.btn-link(type='submit', data-toggle='modal', data-target='#deleteCommunityView') Delete Community View
    #deleteCommunityView.modal.fade(tabindex='-1', role='dialog', aria-labelledby='deleteCommunityViewLabel')
        .modal-dialog(role='document')
            .modal-content
                .modal-header
                    button.close(type='button', data-dismiss='modal', aria-label='Close')
                        span(aria-hidden='true') ×
                    h4#deleteCommunityViewLabel.modal-title #{'Are you sure you want to delete '+data.community_views._id+'?'}
                .modal-body
                    p This cannot be undone
                .modal-footer
                    form.form-group.row(method='post', action='/admin/community-views/'+data.community_views._id)
                        input(type='hidden', name='action', value='deleteCommunityView')  
                        button.btn.btn-danger(type='submit', name='deleteCommunityView') Delete
                        button.btn.btn-default(type='button', data-dismiss='modal') Close
